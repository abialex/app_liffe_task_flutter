name: app_liffe_task_flutter
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  get_it: ^7.7.0
  bloc: ^8.1.3
  flutter_bloc: ^8.1.4
  flutter_svg: ^2.0.10+1

  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.9
  json_serializable: ^6.7.1
  freezed: ^2.5.2

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/icons/
